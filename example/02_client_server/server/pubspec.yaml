name: server_example
description: Data Service server example with VaultRegistry
version: 1.0.0
publish_to: none

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  dart_vault:
    path: ../../..
  aq_schema:
    path: ../../../../aq_schema
