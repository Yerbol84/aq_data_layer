name: serverless_desktop_example
description: Serverless desktop example using dart_vault with InMemory storage
version: 1.0.0
publish_to: none

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  dart_vault:
    path: ../..
  aq_schema:
    path: ../../../aq_schema
