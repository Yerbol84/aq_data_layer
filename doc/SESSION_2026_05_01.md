# Отчёт сессии — 2026-05-01

**Пакет:** dart_vault / aq_data_layer  
**Статус:** ✅ Готово к пушу  
**Тестов пройдено:** 100/100  
**dart analyze:** 0 errors, 0 warnings

---

## Что было сделано

### 1. Artifacts Layer (завершение)

Слой файлового хранилища был начат в предыдущей сессии. В этой сессии завершён:

- `StoredArtifact` — добавлены поля `IndexingStatus`, `chunkCount`, `indexedStoreId`, `indexedAt`, `indexingError`, метод `copyWith()`
- `DocumentAnnotation` — добавлены `indexFields`, `jsonSchema`, `softDelete` (требование AOT компилятора)
- `server/main.dart` — добавлен `artifactBackend` через `ARTIFACT_PATH` env var
- `docker-compose.yml` — один сервер для всех сценариев, volume `artifacts_data`
- Сценарий `main_artifacts.dart` — 6 тестов, все проходят

### 2. Vector Search Layer (полная реализация)

#### Модели и интерфейсы (aq_schema)

| Файл | Описание |
|---|---|
| `data_layer/vector/chunk_span.dart` | Позиция чанка: text offset, time, page, frame |
| `data_layer/vector/pipeline_stamp.dart` | Data lineage — полная история создания чанка |
| `data_layer/vector/extracted_content.dart` | Промежуточная модель после извлечения (in-memory) |
| `data_layer/vector/content_chunk.dart` | Чанк после разбивки (in-memory) |
| `data_layer/vector/vector_point_payload.dart` | Типизированный payload VectorEntry |
| `data_layer/vector/indexing_result.dart` | Результат индексации |
| `data_layer/pipeline/i_content_extractor.dart` | Интерфейс извлечения контента |
| `data_layer/pipeline/i_modality_transformer.dart` | Интерфейс трансформации модальности |
| `data_layer/pipeline/i_chunker.dart` | Интерфейс разбивки на чанки |
| `data_layer/pipeline/i_embeddings_client.dart` | Интерфейс эмбеддингов (с embedBatch) |
| `data_layer/pipeline/i_reranker.dart` | Интерфейс реранкера |
| `data_layer/pipeline/i_vector_store_registry.dart` | Реестр хранилищ + VectorStoreDescriptor |
| `data_layer/pipeline/indexing_pipeline.dart` | Конфигурация pipeline |
| `data_layer/storable/indexing_pipeline_record.dart` | DirectStorable — справочник pipeline |
| `data_layer/storable/vector_store_record.dart` | DirectStorable — справочник store |

#### Реализации (aq_data_layer)

| Файл | Описание |
|---|---|
| `lib/vector/mock_embeddings_client.dart` | Детерминированные эмбеддинги из hashCode |
| `lib/vector/mock_chunker.dart` | Fixed-size chunker для тестов |
| `lib/vector/sentence_chunker.dart` | Разбивка по предложениям с overlap (+21% качество) |
| `lib/vector/plain_text_extractor.dart` | Extractor для text/plain, text/markdown |
| `lib/vector/passthrough_reranker.dart` | No-op reranker |
| `lib/vector/ollama_embeddings_client.dart` | HTTP к Ollama `/api/embeddings` |
| `lib/vector/ollama_reranker.dart` | Cross-encoder через Ollama generate API |
| `lib/vector/vector_store_registry_impl.dart` | Реестр хранилищ |
| `lib/storage/vector_repository_impl.dart` | Полный pipeline оркестратор |
| `lib/storage/pg_vector_storage.dart` | pgvector + tsvector + hybrid search |
| `lib/client/remote/remote_vector_storage.dart` | Клиентский RPC транспорт |

#### Расширения интерфейсов

**`VectorStorage.search()`** — добавлены `sparseQuery` и `alpha` для hybrid search:
```dart
// Pure dense (default, alpha=1.0)
storage.search(collection, vector, tenantId: t)

// Hybrid: 70% dense + 30% BM25
storage.search(collection, vector, tenantId: t, sparseQuery: 'SQL injection', alpha: 0.7)
```

**`IDataLayer`** — мультитон с именованными инстансами:
```dart
// Default (как раньше)
await initializeDataLayer(endpoint: '...');
IDataLayer.instance.direct<MyModel>(...)

// Named instance
await initializeDataLayer(endpoint: '...', key: 'analytics');
IDataLayer.named('analytics').direct<Event>(...)

// Disconnect
await IDataLayer.disconnect();           // default
await IDataLayer.disconnect('analytics'); // named
await IDataLayer.disconnectAll();         // все
```

### 3. Инфраструктура

- **Ollama** как сервис в docker-compose (`ollama/ollama:latest`)
- **pgvector** (`pgvector/pgvector:pg15`) вместо `postgres:15-alpine`
- **Один стек**: `postgres` + `ollama` + `server` — постоянно работают
- `server/main.dart` — `VECTOR_DIM` и `VECTOR_EMBEDDER_ID` env vars
- `VaultRegistry` — добавлен `vectorRegistry` параметр + 4 RPC операции

### 4. Сценарии (14 образов)

| Образ | Тесты | Что проверяет |
|---|---|---|
| `stack-artifacts` | 6/6 ✅ | Upload, metadata, annotations, history, download |
| `stack-vector` | 10/10 ✅ | Full pipeline, remote RPC, status tracking |
| `stack-knowledge` | 7/7 ✅ | Upload+index+annotate+search+delete |
| `stack-rag_basic` | 5/5 ✅ | Ollama nomic-embed-text, real semantic scores |
| `stack-search_core` | 17/17 ✅ | Dense, filter, tenant isolation, score normalization |
| `stack-search_precision` | 5/5 ✅ | P@1=1.0, P@3=1.0, MRR=1.0, Hybrid≥Dense |
| `stack-search_lifecycle` | 7/7 ✅ | Index→Reindex→Delete, status tracking |
| `stack-search_concurrent` | 5/5 ✅ | 10 docs parallel, 910ms, no duplicates |
| `stack-search_chunking` | 4/4 ✅ | SentenceChunker(0.878) > MockChunker(0.724) |
| `stack-search_reranker` | 4/4 ✅ | PassthroughReranker, OllamaReranker |
| `stack-integration_rag` | 5/5 ✅ | Context assembly, hybrid search |
| `stack-integration_annotations` | 6/6 ✅ | chunkId→annotation→span, history |
| `stack-search_migration` | 6/6 ✅ | Mock→Ollama migration, two stores coexist |
| `stack-perf_latency` | 4/4 ✅ | p50=21ms, p95=60ms, hybrid p95=63ms |
| **ИТОГО** | **100/100** ✅ | |

---

## Метрики качества поиска

| Метрика | Значение |
|---|---|
| Precision@1 | **1.0** (5/5) |
| Precision@3 | **1.0** (5/5) |
| MRR | **1.0** |
| SentenceChunker vs MockChunker | **+21%** (0.878 vs 0.724) |
| Search latency p50 | **21ms** |
| Search latency p95 | **60ms** |
| Hybrid search p95 | **63ms** |
| Parallel index (10 docs) | **910ms** |

---

## Документация создана

| Файл | Описание |
|---|---|
| `doc/SESSION_2026_05_01.md` | Этот файл |
| `doc/vector/ARCHITECTURE.md` | Полная архитектура vector layer |
| `doc/vector/PLAN.md` | План реализации (этапы 0-5) |
| `doc/vector/REPORT.md` | Отчёт по реализации vector layer |
| `doc/scenarios/SCENARIOS.md` | Описание всех 15 сценариев |
| `doc/scenarios/PLAN.md` | План реализации сценариев |
| `doc/scenarios/REPORT.md` | Финальный отчёт по сценариям |
| `doc/tech_debt/TECH_DEBT.md` | 14 пунктов технического долга |
| `doc/use_cases/VECTOR_USE_CASES.md` | Use cases для векторной БД |

---

## Готовность к пушу

### ✅ Проверено

- `dart analyze lib/` → **0 errors, 0 warnings**
- Все 100 тестов проходят на реальном стеке
- Обратная совместимость сохранена (`IDataLayer.instance` работает как раньше)
- Все новые компоненты экспортированы из `dart_vault.dart` и `server.dart`
- `AqDomains.all` обновлён (IndexingPipelineRecord, VectorStoreRecord)

### ⚠️ Перед пушем проверить

1. Версию пакета в `pubspec.yaml` — текущая `0.4.0`, рекомендуется `0.5.0`
2. `CHANGELOG.md` — добавить запись о новых возможностях
3. Секреты не попали в коммит (`.env` файлов нет, токены только в тестовых константах)
4. `docker-compose.yml` — `SECURITY_MODE: mock` подходит для dev, не для production

### 🔴 Не включать в пуш

- `doc/archive/dart_vault_dump.md` (378KB) — большой файл, не нужен в репо
- Старые отчёты в `doc/archive/` — можно оставить, но засоряют репо
